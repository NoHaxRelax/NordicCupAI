# Drone baseline and tracking update, 17 September 2026

The RTX 4080 detector completed all 50 epochs successfully. Its final model is available on mypc and in the local preparation workspace, with matching SHA256. W&B support is prepared for future runs; this completed run remains in the original JSON/Git records.

| Check | Result |
| --- | --- |
| Run | `mypc-yolo26x-20260917-v2` |
| Model and data | YOLO26x, frozen dataset v2, 960 input, batch 2 |
| Exact training code | `4d1445a3b9bc47d2947ecefa2dacacd48dfcaa91` |
| Start and finish, Europe/Copenhagen | 17:44:50 to 17:58:21, about 13.5 minutes |
| Peak training GPU allocation | 6.94 GiB |
| Final checkpoint | Fixed epoch 50 `last.pt`, 118,396,958 bytes |
| SHA256 | `14f34b0b97f228342f0b71c5e8fb2c603d0100abc725aa74b675458c3b7f5897` |
| Final model inference, RTX 4080 | p50 20.73 ms, p95 21.12 ms, p99 21.22 ms |

The latency check used 20 warmups and 50 batch-one samples on ten organizer images. It includes PNG decode, model preprocessing/inference/postprocessing, and CPU boxes/JSON. It excludes HTTP, network, camera and tracking logic. The result leaves headroom against 333 ms, but the complete serving endpoint still needs a timing check.

There is no independent fully labeled detector holdout yet. Training-fit mAP would not establish real-world recognition quality, and it did not select this checkpoint. The crop classifier's development holdout covers only six classes. The next accuracy work needs reviewable predictions and a suitable labeled holdout.

## Future W&B runs

New code supports `nordic-ai-cup-drone` with offline logging by default and explicit workspace selection for online mode. It records losses, per-zoom/per-class classifier metrics, timing, GPU allocation, hyperparameters, code/data provenance, and result metadata. Detector training-fit metrics have their own label. Checkpoint uploads are optional; raw training images are not uploaded by these scripts.

Eight CPU-only tracking checks passed, including a real offline run using W&B 0.30.0. Provider-failure handling, offline fallback, destination validation and disabled mode were checked. Python compilation, Bash syntax, Windows PowerShell parsing and both launchers' missing-entity guards passed. No extra GPU training was launched for these checks.

The private [W&B project](https://wandb.ai/badecar-danmarks-tekniske-universitet-dtu/nordic-ai-cup-drone) has been created in the verified academic workspace. SDK authentication on each training machine remains to be configured or verified before online logging; signing into the browser alone does not provide it. See [TRACKING.md](TRACKING.md).

## HPC status

DTU SSH authentication has expired, so the current state of jobs 29428577 and 29428578 is unverified. The latest bounded check returned `Permission denied (publickey,password)`. Restore the existing login from a terminal using `ssh -i ~/.ssh/gbar hpc` and keep the session open. Credentials must stay out of chat and the repository.

The original HPC detector snapshot contains the loss-logger bug fixed in the successful PC run. Once access is restored, inspect that exact job before replacing a pending or failed detector with the fixed immutable snapshot. The independent classifier should remain untouched. A repair of the original run should keep W&B disabled.

Evidence: `receipts/mypc-final-status.json`, `mypc-final-result.json`, `mypc-final-backup.json`, and `mypc-final-smoke.json`. The earlier failed PC attempt and first-epoch backup remain documented separately.
