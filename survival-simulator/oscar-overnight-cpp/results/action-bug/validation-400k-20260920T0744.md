# Clean 400k validation timing probe

Validation attempt `68c44bb352da413abde5e52298a912b4` was submitted at 07:44:51 UTC on 20 September 2026. It used a single, zero-movement, zero-turn, non-spawning duplicate-action burst for one agent at simulated time 0.1. Harvesting remained disabled while no-op capacity mode was active. The endpoint retained the ordinary-score guard at 2,400 with a 130-point reserve.

The endpoint logged the next simulator callback at simulated time 0.2 exactly 15,119.303 ms after it had fully serialized the burst response. The measured interval covers response transfer, the remote receive/decode/validation/action-loop path, the next simulator step, and the following request transfer. It does not include endpoint JSON response construction.

The validation started at 07:44:51.886 UTC and finished at 07:55:38.805 UTC, with score 2145.617196513022 and no errors. The endpoint emitted exactly one 400k burst. This is a single clean measurement for this payload, not evidence that larger payloads are safe.
