[Service]
# Capacity-only validation mode. The endpoint emits one zero-cost no-op burst
# before predators are present; harvesting is suppressed in code while set.
Environment=NIGHT_NOOP_BURST=400000
Environment=NIGHT_NOOP_MAX_BURSTS=1
Environment=NIGHT_NOOP_LATEST_TIME=5
