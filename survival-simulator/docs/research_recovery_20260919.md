# Recovery prepared on 2026-09-19

**STOPPED by user request on 2026-09-19:** all eight research Pods were verified
`EXITED`. The optimization coordinator and background producer were halted,
and the shared queue has a `STOP` marker. Do not restart without new user
authorization. Completed evidence remains on network volume `u9kre0adae`.
The deployment descriptions below are historical.

**Current operation:** the user subsequently authorized spending beyond $40 and
requested continuously useful CPU work. The shared queue described in
[research_throughput_20260919.md](research_throughput_20260919.md) supersedes the
four-Pod budget and 2.7-hour cutoff below. Current launcher/coordinator PIDs are
10508/10509, completion guard PID 9112. Earlier deployment details remain as
history. The original frozen protocol and accepted policy were preserved.

Status: resumed on 2026-09-19 at 06:54:54 UTC (08:54:54 Paris), at generation 2's
opening boundary. The user explicitly authorized credential installation on the
replacement Pod. Both files are installed outside the research tree with mode
0600, and Codex verified the ChatGPT login. The independent shutdown guard is
armed. The current four-Pod fleet guard is PID 7327, launcher PID 7376, and
coordinator PID 7377. Prior launchers/coordinators and guard PID 2081 are retired.
The two temporary local setup-guard processes were stopped after verification,
so their former 08:16 UTC deadline cannot interrupt this campaign.

The first two reviewer calls exposed a missing `codex-code-mode-host` binary in
the replacement container. They returned explicit blocked/no-change proposals;
their artifacts remain recorded and are not policy-performance evidence.
The matching official 0.155.1 helper was installed and its release SHA-256
verified (`codex-helper-install.json` under `/workspace/recovery-ops`). A subsequent
generation 2 major review successfully read the context with the repaired tool.
The external launcher now checks that this helper exists and is executable.

The generation 2 major review reached its 900-second limit before returning its
structured proposal. Its fallback search then failed before creating any cases:
the default dimensions included `trapping_orchard.hungry`, explicitly classified
as unused/non-tunable in the current catalog. The coordinator was paused for a
controlled repair; failed attempts and the interrupted g3.1 reviewer were archived
under `operator-controls/cli-search-repair/`, with all ten review calls retained
in accounting. No completed simulation case was discarded.

At 07:22:37 UTC, an external coordinator adapter restarted generation 2's failed
BO step. `research_recovery_runtime.py` filters only fallback parameter lists
against the candidate's catalog, preserving strict validation for other explicit
agent choices. It allows future reviews up to 1800 seconds, clipped by the same
funded development budget/final reserve, and supplies guidance to spend about
five minutes gathering evidence before completing the structured proposal.
Three targeted fallback-validation tests passed. No frozen source, configuration,
candidate, evaluator, score or final panel was changed. The hashed adapter and
handoff receipts are `operator-controls/runtime-adapter.json` and
`operator-controls/runtime-handoff.json`, also copied to the local launch folder.

At approximately 07:23 UTC the live dashboard verified four advancing C++ cases
on fresh screen seeds 200064-200067, at about 80-97 simulated seconds after 26
wall seconds. These are partial progress observations, not performance results.
The search has 24 trials and up to 16 concurrent game slots; its first batch is
the incumbent anchor. The timed-out structural proposal was not accepted.

Latest source refresh: see [upstream refresh](upstream_refresh_20260919.md).
Lucas is synced through `37a3418f` (06:47 UTC), including the new bait logic and
its unresolved continuity findings. An isolated raw overlay failed a required
configuration-interface check; the reviewer must adapt it selectively. Latest
Oscar C++ policy and wall-map sources are also supplied as pinned references.

## Confirmed causes

The old supervisor records `limit_reason: Storage limit reached`. Development
had a 9 GiB allowance after reserving 5 GiB of its 14 GiB campaign cap; another
2.125 GiB was reserved for active workers. This triggered well before the $40
financial limit. Its directory scanner also performed separate existence and
size checks: a temporary file could be renamed between them. The two recorded
missing temporary-file errors match this race.

The old final assessment attempted all 32 games. Technical status inspection
found 31 completed games and one wall-time-limit error. No held-out survival or
score values were inspected. That panel is now retired; its original artifacts,
selection and campaign remain unchanged.

## Prepared continuation

- Pod: `a0aruuldun08t9`, created `2026-09-19T06:08:06.38Z`.
- SSH: `root@213.173.105.83`, port `59594`; pinned in local `known_hosts`.
- Source: `/workspace/research-recovery-source`.
- Campaign: `/workspace/research-resume-20260919`.
- Parent: `/workspace/research-night-1`, retained intact.
- Volume: `u9kre0adae`, increased from 30 to 100 GB.
- Start with generation 2's Lucas boundary comparison, then subgeneration 2.1.
- Target major generation 8. Stagnation can shorten subgenerations after two;
  it does not end the whole campaign before major 8. Four subgenerations maximum.
- Eight focused trials per subgeneration and 24 broader BO trials per major;
  measured step deadlines still bound searches. Sixteen game slots remain.
- Each round has four preassigned screening maps, eight independent Python
  confirmation maps, and a twelve-map major confirmation panel. Panels rotate
  across rounds. Old holdout seeds 1001–1016 are excluded. The new sixteen-map
  holdout stays sealed until final selection.
- The accepted policy and original baseline retain their exact policy bytes,
  in new snapshots that include the repaired infrastructure. Old candidates,
  original IDs, scores, journal, and migration hashes remain available.
- Atomic writers use unique temporary files. Storage scans tolerate files that
  disappear during enumeration and still report permission and other failures.
- Recoverable case errors have up to two retries; successful games are never
  rerun. Retry counts survive coordinator restart and wall time increases for
  timeout retries. Policy errors and poor survival are not retry criteria.
- Technical reviewer failures have two retries, coordinator I/O failures five.
- The base case timeout increases from one to two hours, with a four-hour final
  evaluation reserve.
- Reviewer: pinned Codex CLI 0.155.1, `gpt-6-astra`, `xhigh`, ChatGPT-only login.
  The cumulative review-call cap is 48, including the six historical calls.
  Subscription reviews have zero API-spend reservation; no API key is configured.

## Budget and startup

The $40 total remains. The continuation accounts for $10 of earlier spending,
reserves $5, and budgets the main Pod plus storage at $0.985/hour. Following the
four-Pod request, $8 is reserved for three auxiliary Pods. The main Pod's effective
17.25-hour deadline is measured from creation, including setup:
**2026-09-19 23:23:06 UTC**. The conservative maximum is $39.99125 including the
reserve. The frozen protocol is unchanged; the external runtime applies this
audited budget reservation. Financial and
storage limits remain binding; the system cannot promise eight generations
under every possible runtime or provider outage.

The separate external launcher is
`/workspace/recovery-ops/start_research_recovery.py`. It refuses to launch without
successful trusted checks, a verified ChatGPT login, the private shutdown key,
the correct Pod identity and an armed independent shutdown guard. Run it with
`/workspace/predator-search-venv/bin/python -B` after authorization and installation.
Credential destinations are `/root/.codex/auth.json` and
`/root/.config/nordiccup/runpod-api-key`, mode 0600, outside the research tree.

The former local setup guard has been disabled; processes 33608 and 37040 were
matched by command line, stopped and verified absent. Its historical records are
`runs/runpod-launch-20260918/recovery-setup-shutdown.*`. The independent Pod guard
now owns shutdown at launcher completion or the funded hard deadline above.

The remote `launch-receipt.json` is saved locally as `recovery-launch-receipt.json`;
`remote-state.json` has updated PID/auth metadata. The dashboard collector is
connected and shows the running continuation. Inspect current stage and active
case counts before claiming that simulation games, rather than checks or a
coding review, are active.

Do not re-run the initial launcher for this running campaign. Its launch receipt
is deliberately retained. The active command uses the external adapter:
`python -B -u /workspace/recovery-ops/research_four_pod_runtime.py --source
/workspace/research-recovery-source --out /workspace/research-resume-20260919
--fleet-controls /workspace/research-resume-20260919/operator-controls/four-pod-budget.json`.
`launch-four-pods.sh` retains the completion trap watched by the fleet guard.

## Four active Pods

The additional studies started at 07:46 UTC on the same 100 GB shared volume,
with the same pinned image, Python environment, and frozen incumbent snapshot.
All four Pods have 32 vCPUs and cost $0.96/hour each for compute. Each study permits
16 concurrent games; this is three independent studies alongside the main BO,
not a distributed queue for one BO study.

| Study | Pod | Screening / Python comparison seeds |
| --- | --- | --- |
| Main generation BO | `a0aruuldun08t9` | Existing rotating round panels |
| Guide delivery and trap geometry | `nc6e1yh96rqylc` | 500000–500003 / 500010–500017 |
| Bait continuity and safer replacement | `3ziz66qp102nyh` | 500100–500103 / 500110–500117 |
| Capture versus feeding allocation | `jluij0qgnz2tys` | 500200–500203 / 500210–500217 |

Each additional study permits 24 focused C++ trials, then paired Python
confirmation on eight independent maps. Plans are immutable and registered by
SHA-256 in `/workspace/research-capture-fleet-20260919/fleet.json`. New panels were
checked against all development panels and both current and retired holdouts.
The studies provide advisory evidence; the main supervisor retains promotion
authority. Scoped Codex guidance points subsequent reviews to these reports and
requires normal validation of promising additions or combinations.

Auxiliary Pods stop on completion or 2.7 hours after their creation (approximately
10:26–10:28 UTC). The controller's `runpod_fleet_guard.py` covers all four IDs and
stops auxiliary Pods before itself. Worker Pods have no ChatGPT or Runpod API
credentials. A separate local deadline-only backup runs while this computer stays
awake; its configuration and journal are `four-pod-guard.json` and
`four-pod-backup-guard.jsonl` under the Git-ignored launch folder.

The dashboard reads the `operator-controls/active-fleet.json` pointer, verifies
registered plan hashes, and excludes holdout seeds before reading study results.
It distinguishes the $8 reservation from accrued worker costs and shows the
effective main deadline. Nineteen guard, runtime and dashboard tests passed,
including fresh auxiliary panels and rejecting held-out or modified plans.
Live progress was verified with sixteen advancing games on each auxiliary Pod
and ongoing games on the main Pod. The final holdout remains unopened.

## Verification and upstream

47 relevant local tests passed, plus 12 guard/dashboard tests after the external
helper updates. An additional scheduler check confirmed all eight major reviews
occur despite stagnant rounds, while subgeneration early stopping remains.
On Runpod, both the original baseline and accepted policy passed all 126 trusted
tests. `recovery-preflight.json` and `migration.json` record the checks and
preserved parent hashes.

The Lucas bridge was refreshed to `594fe799d44fdb7f5dfe1c9049f2f5539be415cb`
at 06:34:33 UTC. The new commits add replay tooling and measured evidence;
the models-directory diff from the formerly integrated `53f1a4c...` is empty.
The prepared campaign will perform its normal boundary comparison. A local
five-minute bridge refresh is running while the computer remains awake.

The dashboard at http://127.0.0.1:8765 now points to the prepared continuation.
The prior cached report is retained as `dashboard-before-recovery.json`.
