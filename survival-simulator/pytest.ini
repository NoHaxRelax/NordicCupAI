[pytest]
# The merged oscar-trap-research/ tree vendors its own simulator copy and
# ships research tests that expect their own rootdir on sys.path. Collecting
# them from here breaks a bare `pytest` run, so scope collection to our suite.
testpaths = tests
norecursedirs = oscar-trap-research .venv .venv-hpc runs __pycache__ .git
