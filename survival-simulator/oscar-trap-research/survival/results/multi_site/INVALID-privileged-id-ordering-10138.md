The exploratory 1-second `47fb1383` and 300-second `420bbb72` runs reordered
bait IDs during setup using the hidden fixture guide coordinates. Their native
frames remain inspectable, but they are invalid evidence for DTO-only site
selection and are excluded from all results. The harness was corrected before
any further run so bait IDs follow deterministic static-map selector order.
