---
title: nordic-probe
emoji: 🧪
colorFrom: gray
colorTo: gray
sdk: docker
app_port: 7860
pinned: false
---

Table-lookup endpoint plus the validation label miner. See `app.py`. Secrets: NORDIC_API_KEY, CONTROL_TOKEN, HF_TOKEN, STATE_REPO. Upload `base_answers.json` to the state repo before the first start.
